{
  "frontend/src/index.css": "Global styles with deep navy and gold OKLCH color palette, Barlow Condensed display font and Barlow body font for a bold athletic sports-team aesthetic, applied to both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom navy and gold color scales, Barlow font families, gold glow box shadows, and fade-in-up animation keyframes for the sports theme.",
  "frontend/src/hooks/useQueries.ts": "Custom React Query hooks for backend interactions — includes `useSubmitSignUp` mutation, `useGetAllSignUps` query, `useIsCallerAdmin` query (calls `isCallerAdmin()` to check admin role), and exports position capacity constants.",
  "frontend/src/components/Layout.tsx": "Full-page layout wrapper with sticky navy header that now includes a \"Sign-Ups\" navigation link (visible to all users) pointing to `/signups`, plus the conditional \"Admin\" link for authenticated admins, main content area, and branded footer.",
  "frontend/src/components/HeroSection.tsx": "Hero banner section with stats bar showing position availability; Guard now displays \"1 Slot Open\" (singular) using the `GUARD_MAX_CAPACITY` constant, while Forward and Center continue to show \"2 Slots Open\" using their respective capacity constants.",
  "frontend/src/pages/SignUpPage.tsx": "Main sign-up page with react-hook-form; Guard position now shows \"1 slot\" badge and uses `GUARD_MAX_CAPACITY` (1) in capacity-full error messages, while Forward and Center retain their 2-slot capacity badges and messaging.",
  "frontend/src/App.tsx": "Root app component using TanStack Router with four routes — `/` renders `SignUpPage` inside `Layout`, `/signups` renders the new `SignUpsPage`, `/owner` renders `OwnerDashboard`, and `/admin` renders `AdminPage`.",
  "frontend/src/pages/OwnerDashboard.tsx": "Owner-only admin dashboard page at `/owner` that gates access behind Internet Identity login; authenticated users see a full table of all player sign-ups (name, email, phone, age, position, experience level) with loading skeletons, empty state, error handling for unauthorized access, and a refresh button.",
  "frontend/index.html": "HTML entry point with updated page title \"Blue Panthers – NCAA Sign-Up\" and a meta description referencing the NCAA basketball program.",
  "frontend/src/pages/AdminPage.tsx": "Admin-only page at `/admin` that gates access behind Internet Identity login and an admin role check via `useIsCallerAdmin`; authenticated admins see a full table of all player sign-ups with position summary cards, loading skeletons, and error handling, styled in the navy/gold Blue Panthers theme.",
  "frontend/src/pages/SignUpsPage.tsx": "New protected page at `/signups` that shows a sign-in prompt to unauthenticated users and displays a full table of all Blue Panthers player sign-ups (name, email, phone, age, position, experience) to authenticated users, with loading skeletons, error handling, and a refresh button."
}